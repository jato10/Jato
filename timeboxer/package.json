{
  "name": "timeboxer",
  "version": "1.0.0",
  "private": true,
  "description": "Plan your day in 15-minute blocks. One HTML file, no runtime dependencies.",
  "scripts": {
    "test": "node test/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
